# 🏢 Тестовое задание

<div align="center">

</div>

## 🎯 О проекте

Решение тестового задания

Обе задачи реализованы в едином ASP.NET Core Web API проекте

# 🏠 Задача 1: Мониторинг цен квартир

## 💡 Техническое решение

### Ключевое решение: API вместо HTML-парсинга

При анализе сайта обнаружил публичный API: `https://prinzip.su/api/v1/public/apartments/{id}`

**Преимущества перед HTML-парсингом:**
- ✅ В 10 раз надежнее — структурированный JSON
- ✅ Не зависит от изменений дизайна
- ✅ В 5 раз быстрее — прямой запрос без браузера
- ✅ Легче тестировать

---

### Архитектурные решения

#### 1. Dependency Injection через интерфейс

Создан интерфейс `IPrinzipApiService` с методами:
- `ExtractIdFromUrl(string url)` — извлечение ID через Regex
- `GetApartmentInfoAsync(int id)` — получение данных из API

**Преимущества:**
- Легко добавить другие источники без изменения контроллеров
- Возможность подменить на Mock для тестов
- Следование SOLID принципам

#### 2. Background Service для автоматического мониторинга

Класс `PriceMonitorWorker` реализует `BackgroundService`:
- Запускается автоматически при старте приложения
- Проверяет цены каждый час
- Детальное логирование всех операций

#### 3. Entity Framework Core + MS SQL

Модель `Subscription` включает:
- Пользовательская ссылка и извлеченный ID
- Email для уведомлений
- Последняя известная цена и статус
- Метаданные
- Индекс для оптимизации запросов по ApartmentId

---

## 🔌 API Endpoints

### POST /api/subscriptions/subscribe
Создание подписки на мониторинг квартиры.

**Параметры:**
- `url` — ссылка на квартиру prinzip.su
- `email` — email для уведомлений

**Возвращает:** ID подписки, текущую цену и статус квартиры

---

### GET /api/subscriptions/prices
Получение списка всех активных подписок с актуальными ценами.

**Возвращает:** массив подписок с текущими ценами, статусами и временем последней проверки

---

### DELETE /api/subscriptions/{id}
Удаление подписки по ID.

**Возвращает:** подтверждение удаления

---


---

## 🔄 Принцип работы

### Процесс создания подписки:
1. Контроллер валидирует входные данные
2. Извлекает ID квартиры из URL через Regex
3. Запрашивает информацию через API prinzip.su
4. Проверяет отсутствие дубликата в БД
5. Сохраняет подписку в БД
6. Возвращает результат с текущей ценой

### Фоновый мониторинг:
1. Worker запускается автоматически при старте
2. Каждый час загружает все подписки из БД
3. Для каждой подписки запрашивает актуальную цену
4. Сравнивает с сохраненной ценой
5. При изменении логирует и [TODO] отправляет Email
6. Обновляет LastPrice и LastCheckedAt в БД

---

# ⚙️ Задача 2: Многопоточный сервер

## 🔧 Реализация

Статический класс `Server` использует **ReaderWriterLockSlim**.

### Метод GetCount()
- Входит в ReadLock
- Возвращает текущее значение count
- Освобождает ReadLock в блоке finally
- **Множество потоков читают параллельно**

### Метод AddToCount(int value)
- Входит в WriteLock
- Изменяет значение count
- Освобождает WriteLock в блоке finally
- **Только один поток пишет в момент времени**

---

## 🔌 Демо API

### GET /api/servertest/count
Получить текущее значение счетчика.

### POST /api/servertest/add?value={число}
Добавить значение к счетчику.

### POST /api/servertest/stress-test
Стресс-тест многопоточности:
- 100 потоков параллельно читают (100,000 операций)
- 10 потоков последовательно пишут (1,000 операций)
- Проверка отсутствия race conditions

**Успешный результат:** `expectedCount == actualCount == 1000`

---

# 🚀 Быстрый старт

## ✅ Требования

- .NET 9.0 SDK
- MS SQL Server или SQL Server Express
- Git


